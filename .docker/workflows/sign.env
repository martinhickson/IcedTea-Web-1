# Signing credentials and git source for .docker/workflows/sign.ps1
#
# sign.ps1 git-clones a fresh checkout on the host (like GitHub Actions checkout),
# then bind-mounts that clone into the Windows container for build and signing.

# Git source (use GIT_REMOTE_URL or GITHUB_REPOSITORY)
GITHUB_REPOSITORY=martinhickson/IcedTea-Web-1
GIT_REF=icedtea-web-2.6.2
# GIT_REMOTE_URL=https://github.com/martinhickson/IcedTea-Web-1.git
# GIT_TOKEN=

# Fresh clone directory on the host (default: .docker/workflows/.source)
# GIT_CLONE_DIR=C:\build\IcedTea-Web-1

JNLP_JCA_SIGN_CERTCHAIN_HOST_FILE=C:\path\to\signing-chain.pem

KEYVAULT_URL=https://your-vault.vault.azure.net/
AZURE_CLIENT_ID=
AZURE_TENANT_ID=
AZURE_CLIENT_SECRET=
JNLP_JCA_SIGN_ALIAS=
JNLP_JCA_TSA_URL=http://timestamp.digicert.com

# Optional: PEM inline instead of JNLP_JCA_SIGN_CERTCHAIN_HOST_FILE mount
# JNLP_JCA_SIGN_CERTCHAIN=

# Path inside the container (default matches compose volume mount)
JNLP_JCA_SIGN_CERTCHAIN_FILE=C:\signing\certchain.pem

# Optional build settings
# ITW_VERSION=2.0.1-SNAPSHOT
# ITW_DOTNET_SELF_CONTAINED=true
# ITW_CORRETTO_URL=https://corretto.aws/downloads/latest/amazon-corretto-11-x64-windows-jdk.zip

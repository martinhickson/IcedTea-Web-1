<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>net.sourceforge.icedtea-web</groupId>
    <artifactId>icedtea-web-parent</artifactId>
    <version>2.0.1-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>itw-localhost-connect-it</artifactId>
  <packaging>jar</packaging>

  <name>IcedTea-Web localhost connect IT</name>
  <description>Standalone Failsafe module that reproduces SecurityException on http://localhost</description>

  <organization>
    <name>IcedTea-Web</name>
    <url>https://github.com/martinhickson/IcedTea-Web-1</url>
  </organization>

  <properties>
    <maven.compiler.release>11</maven.compiler.release>
    <maven.compiler.source>11</maven.compiler.source>
    <maven.compiler.target>11</maven.compiler.target>
    <maven.failsafe.plugin.version>3.2.5</maven.failsafe.plugin.version>
    <version.junit>5.10.2</version.junit>
    <version.undertow>2.3.18.Final</version.undertow>
    <webstart.plugin.version>1.2.4-bravura</webstart.plugin.version>
    <itw.uberjar.classifier>uber</itw.uberjar.classifier>
    <itw.jdk11.home>${jdk11.home}</itw.jdk11.home>
    <itw.launch.timeout.seconds>180</itw.launch.timeout.seconds>
    <itw.signed.app.jar>${project.build.directory}/jnlp/${project.artifactId}-${project.version}.jar</itw.signed.app.jar>
  </properties>

  <dependencies>
    <dependency>
      <groupId>net.sourceforge.icedtea-web</groupId>
      <artifactId>icedtea-web</artifactId>
      <classifier>${itw.uberjar.classifier}</classifier>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${version.junit}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.undertow</groupId>
      <artifactId>undertow-core</artifactId>
      <version>${version.undertow}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration combine.self="override">
          <release>11</release>
          <encoding>UTF-8</encoding>
          <fork>false</fork>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.3.0</version>
        <configuration>
          <archive>
            <manifest>
              <mainClass>net.sourceforge.jnlp.it.localhost.LocalhostConnectApp</mainClass>
            </manifest>
          </archive>
        </configuration>
      </plugin>

      <plugin>
        <groupId>io.github.martinhickson</groupId>
        <artifactId>webstart-maven-plugin</artifactId>
        <version>${webstart.plugin.version}</version>
        <executions>
          <execution>
            <id>sign-localhost-app</id>
            <phase>package</phase>
            <goals>
              <goal>jnlp-single</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <skipDependencies>true</skipDependencies>
          <makeArchive>false</makeArchive>
          <addApplicationFile>false</addApplicationFile>
          <verbose>true</verbose>
          <jnlp>
            <outputFile>localhost-connect.jnlp</outputFile>
            <mainClass>net.sourceforge.jnlp.it.localhost.LocalhostConnectApp</mainClass>
          </jnlp>
          <sign>
            <keystore>${project.build.directory}/itw-localhost-keystore.jks</keystore>
            <storepass>changeit</storepass>
            <keypass>changeit</keypass>
            <alias>itw-localhost</alias>
            <keyalg>RSA</keyalg>
            <validity>3650</validity>
            <dnameCn>itw-localhost-connect-it</dnameCn>
            <dnameOu>ITW</dnameOu>
            <dnameO>IcedTea-Web</dnameO>
            <dnameL>Test</dnameL>
            <dnameSt>Test</dnameSt>
            <dnameC>US</dnameC>
            <verify>true</verify>
            <keystoreConfig>
              <gen>true</gen>
              <delete>true</delete>
            </keystoreConfig>
          </sign>
          <pack200>
            <enabled>false</enabled>
          </pack200>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.6.0</version>
        <executions>
          <execution>
            <id>copy-uber-jar</id>
            <phase>pre-integration-test</phase>
            <goals>
              <goal>copy</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>net.sourceforge.icedtea-web</groupId>
                  <artifactId>icedtea-web</artifactId>
                  <version>${project.version}</version>
                  <classifier>${itw.uberjar.classifier}</classifier>
                  <type>jar</type>
                  <overWrite>true</overWrite>
                  <outputDirectory>${project.build.directory}</outputDirectory>
                  <destFileName>icedtea-web-${itw.uberjar.classifier}.jar</destFileName>
                </artifactItem>
                <artifactItem>
                  <groupId>net.bytebuddy</groupId>
                  <artifactId>byte-buddy-agent</artifactId>
                  <version>${bytebuddy.version}</version>
                  <type>jar</type>
                  <overWrite>true</overWrite>
                  <outputDirectory>${project.build.directory}/bin</outputDirectory>
                  <destFileName>byte-buddy-agent.jar</destFileName>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.1.1</version>
        <executions>
          <execution>
            <id>prepare-javaws-launcher</id>
            <phase>pre-integration-test</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>bash</executable>
              <arguments>
                <argument>${project.basedir}/scripts/prepare-javaws-launcher.sh</argument>
                <argument>${project.build.directory}/bin</argument>
                <argument>${project.build.directory}/icedtea-web-${itw.uberjar.classifier}.jar</argument>
                <argument>net.sourceforge.jnlp.runtime.JavawsUberLauncher</argument>
              </arguments>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>${maven.failsafe.plugin.version}</version>
        <configuration>
          <includes>
            <include>**/*IT.java</include>
          </includes>
          <systemPropertyVariables>
            <itw.javaws.bin>${project.build.directory}/bin/javaws</itw.javaws.bin>
            <itw.jdk11.home>${itw.jdk11.home}</itw.jdk11.home>
            <itw.signed.app.jar>${itw.signed.app.jar}</itw.signed.app.jar>
            <itw.launch.timeout.seconds>${itw.launch.timeout.seconds}</itw.launch.timeout.seconds>
          </systemPropertyVariables>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
